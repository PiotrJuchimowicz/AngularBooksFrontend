import {FontWeightDirective} from './font-weight.directive';

describe('HighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new FontWeightDirective(null);
    expect(directive).toBeTruthy();
  });
});
