import {SortDictionaryCategoriesPipe} from './SortDictionaryCategories.pipe';

describe('FilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SortDictionaryCategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
